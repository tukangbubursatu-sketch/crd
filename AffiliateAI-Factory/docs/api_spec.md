# API Specification

## Endpoints
- `GET /products` menampilkan daftar produk.
- `POST /scripts` membuat skrip baru.
- `POST /videos` membuat data video.
- `POST /analytics` mencatat hasil performa.

## Response Format
Semua response harus mengandung `status`, `message`, dan `data`.
